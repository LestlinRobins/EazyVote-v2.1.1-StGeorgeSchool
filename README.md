I could not add the deployment files (i.e, the exe files and all the things that the exe file depends upon), but you can convert it easily.

1. Open cmd as admin.
2. cd to the project directory.
3. now run 'pyinstaller --onefile [filename].py' for reset, result, stgeorge.py, and sqltable files


I have uploaded the exe files and entire deployement package to google drive in the folder St. George Election Deployement Files
